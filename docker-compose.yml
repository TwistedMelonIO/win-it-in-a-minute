services:
  win-it-in-a-minute:
    build: .
    container_name: win-it-in-a-minute
    ports:
      - "0.0.0.0:4000:3000"
    environment:
      - NODE_ENV=production
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
